# Normalize to LF in the repository so Linux and macOS contributors never see a
# whole-file diff just because the author is on Windows.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.mp4 binary
*.ico binary
